import React from "react";
import ChatList from "../../Components/ChatList/chatList";

const Chat = () => {
    return (
        <div>
            <ChatList />
        </div>
    );
};

export default Chat;
